//@version=6
library("Codex/PivotUtils/1", overlay=false)

// Returns pivot high using ta.pivothigh
export pivotHigh(src, len) =>
    ta.pivothigh(src, len, len)

// Returns pivot low using ta.pivotlow
export pivotLow(src, len) =>
    ta.pivotlow(src, len, len)
